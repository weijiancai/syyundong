v_user_friend;

select user_id,user_head,friend_id,op_user_friend.input_date,nick_name,gender,local_url
from op_user_friend,db_user
left join db_images on (db_images.id = db_user.user_head)
 where friend_id =  db_user.id

v_venue person_cost :varchar 32

db_activity:person_cost:默认值0

v_join_activity :power改写

db_activity:interest_count:默认值0

v_hot_activity:power 改写  interest_count

db_doyen_hall:description:text

db_activity:is_need_verify  改成：is_verify

v_game_activity:power